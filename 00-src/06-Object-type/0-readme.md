# 6. Object Types in TypeScript: Declaring Types for Objects

## Declaring Object Types

- **Syntax to declare an object type:**

```ts
let user: {
  name: string;
  age: number;
  isActive: boolean;
} = {
  name: 'Alice',
  age: 25,
  isActive: true,
};
```
